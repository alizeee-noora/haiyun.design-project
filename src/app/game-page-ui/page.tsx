import { ProjectGallery } from "@/components/ProjectGallery";

export default function GamePageUI() {
  return (
    <ProjectGallery
      folder="merged"
      title="游戏页面 UI 设计"
      subtitle="Game Page UI"
      year="2025"
      description="系统化的游戏界面设计，包含按钮、图标、卡片等可复用组件。"
    />
  );
}